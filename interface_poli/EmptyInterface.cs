using System;
namespace interface_poli
{
	public interface EmptyInterface
	{
        //interface represents a method: makeSound.
        interface IAnimal
        {
            //method signature for makeSound()
            void makeSound();
        }
    }
}

